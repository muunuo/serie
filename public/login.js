// var userForm_ = document.createElement("form");
// userForm_.setAttribute('method',"POST");
// userForm_.setAttribute('action',"/createUser_");

// var username_ = document.createElement("input"); //input element, text
// username_.setAttribute('type',"text");
// username_.setAttribute('name',"username_");

// var password_ = document.createElement("input"); //input element, text
// password_.setAttribute('type',"text");
// password_.setAttribute('name',"password_");

// var nickname_ = document.createElement("input"); //input element, text
// nickname_.setAttribute('type',"text");
// nickname_.setAttribute('name',"nickname_");

// var userSubmit_ = document.createElement("input"); //input element, Submit button
// userSubmit_.setAttribute('type',"submit");
// userSubmit_.setAttribute('value',"submit_");

// userForm_.appendChild(username_);
// userForm_.appendChild(password_);
// userForm_.appendChild(nickname_);
// userForm_.appendChild(userSubmit_);

// document.getElementsByTagName('body')[0].appendChild(userForm_);
